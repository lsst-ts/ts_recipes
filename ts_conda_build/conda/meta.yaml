package:
  name: ts-conda-build
  version: 0.4.4

build:
  noarch: generic

requirements:
  build:
    - python {{ python }}
    - numpy {{ numpy }}
  run:
    - astropy >=5
    - boto3 =1.34
    - jsonschema =4
    - moto =5
    - numpy {{ numpy }}
    - pre-commit =4.1
    - py =1
    - pytest =8.1
    - pytest-asyncio =0.23
    - pytest-cov =4.1
    - python {{ python }}
    - pyyaml =6
    - setuptools {{ setuptools }}.*
    - setuptools_scm {{ setuptools_scm }}.*
    - types-pyyaml =6
